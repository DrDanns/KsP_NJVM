void main(){
	local Integer a;
	local Integer result;
	a = readInteger();
	result = f(a);
	writeInteger(result);
	writeCharacter('\n');
}

Integer f(Integer i){
	if(i == 0) return 1;
	return i * f(i-1);
}